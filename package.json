{
  "name": "aws-steplens",
  "displayName": "AWS StepLens",
  "description": "Visualize, navigate and lint AWS Step Functions definitions (ASL YAML/JSON + JSONata). Interactive graph preview, real-time linter with 50+ rules, nested Parallel/Map sub-graph exploration, and PNG/JPEG export.",
  "version": "0.2.0",
  "galleryBanner": {
    "color": "#1a1a2e",
    "theme": "dark"
  },
  "pricing": "Free",
  "publisher": "almekpoh",
  "icon": "images/icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/almekpoh/aws-steplens"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Linters",
    "Visualization",
    "Other"
  ],
  "keywords": [
    "aws",
    "step functions",
    "stepfunction",
    "step function",
    "statemachine",
    "state machine",
    "asl",
    "serverless",
    "jsonata",
    "workflow",
    "sfn",
    "visualization",
    "graph",
    "linter",
    "diagram",
    "yaml",
    "json",
    "parallel",
    "map"
  ],
  "activationEvents": [
    "onLanguage:yaml",
    "onLanguage:json"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "keybindings": [
      {
        "command": "steplens.preview",
        "key": "ctrl+alt+g",
        "mac": "cmd+alt+g",
        "when": "steplens.isSfnFile"
      }
    ],
    "grammars": [
      {
        "path": "./grammars/jsonata-injection.tmLanguage.json",
        "scopeName": "inline.jsonata.sfn",
        "injectTo": [
          "source.yaml",
          "source.json"
        ]
      }
    ],
    "commands": [
      {
        "command": "steplens.preview",
        "title": "StepLens: Open Graph Preview",
        "icon": "$(type-hierarchy)"
      },
      {
        "command": "steplens.lint",
        "title": "StepLens: Lint Current File"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "steplens.preview",
          "when": "steplens.isSfnFile",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "StepLens",
      "properties": {
        "steplens.autoDetect": {
          "type": "boolean",
          "default": true,
          "description": "Auto-detect Step Functions definitions by looking for a 'States' key"
        },
        "steplens.lintOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Run linter on file save"
        },
        "steplens.lintOnType": {
          "type": "boolean",
          "default": true,
          "description": "Run linter on every keystroke"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run bundle && npm run bundle:webview",
    "bundle": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --minify",
    "bundle:webview": "esbuild webview/vendor-entry.js --bundle --outfile=webview/vendor.js --platform=browser --minify",
    "compile": "tsc -p ./",
    "watch": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --sourcemap --watch",
    "lint": "eslint src --ext ts",
    "test": "node --require ts-node/register --require ./test/setup.ts --test test/aslParser.test.ts test/linter.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "cytoscape": "^3.33.2",
    "cytoscape-dagre": "^2.5.0",
    "dagre": "^0.8.5",
    "esbuild": "^0.25.0",
    "eslint": "^9.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0",
    "yaml": "^2.4.0",
    "@vscode/vsce": "^3.2.1"
  }
}
